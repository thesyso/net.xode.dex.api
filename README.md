# net.xode.dex.api


src/
├── controllers/    # 비즈니스 로직 (함수)
├── routes/         # 엔드포인트 정의 (URL)
├── libs/           # interface, type 등
├── middleware/     # 에러 핸들러 등등
└── index.ts        # 서버 실행 및 미들웨어 연결